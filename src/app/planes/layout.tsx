import { metadata as planesMetadata } from './metadata';

export const metadata = planesMetadata;

export default function PlanesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

