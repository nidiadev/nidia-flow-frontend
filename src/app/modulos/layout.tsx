import { metadata as modulosMetadata } from './metadata';

export const metadata = modulosMetadata;

export default function ModulosLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

