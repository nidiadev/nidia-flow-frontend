import { metadata as funcionalidadesMetadata } from './metadata';

export const metadata = funcionalidadesMetadata;

export default function FuncionalidadesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

